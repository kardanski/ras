function selectalljan(source) {
  checkboxes = document.getElementsByName('danjan[]');
  for(var i=0, n=checkboxes.length;i<n;i++) {
    checkboxes[i].checked = source.checked;
  }
}
function selectallfeb(source) {
  checkboxes = document.getElementsByName('danfeb[]');
  for(var i=0, n=checkboxes.length;i<n;i++) {
    checkboxes[i].checked = source.checked;
  }
}
function selectallmar(source) {
  checkboxes = document.getElementsByName('danmar[]');
  for(var i=0, n=checkboxes.length;i<n;i++) {
    checkboxes[i].checked = source.checked;
  }
}
function selectallapr(source) {
  checkboxes = document.getElementsByName('danapr[]');
  for(var i=0, n=checkboxes.length;i<n;i++) {
    checkboxes[i].checked = source.checked;
  }
}
function selectallmaj(source) {
  checkboxes = document.getElementsByName('danmaj[]');
  for(var i=0, n=checkboxes.length;i<n;i++) {
    checkboxes[i].checked = source.checked;
  }
}
function selectalljun(source) {
  checkboxes = document.getElementsByName('danjun[]');
  for(var i=0, n=checkboxes.length;i<n;i++) {
    checkboxes[i].checked = source.checked;
  }
}